package com.pfe.back.repository;

import com.pfe.back.entity.SystemNode;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SystemNodeRepository extends JpaRepository<SystemNode, Long> {
}
